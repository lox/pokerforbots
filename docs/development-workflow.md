# Development Workflow Guide

This guide explains how to run a development loop for testing and improving poker bots using tmux, the PokerForBots server, and performance metrics.

## Prerequisites

Uses CashApp's Hermit, `source bin/activate-hermit` or call things directly from bin.

## Quick Start

### 1. Start the Server in tmux

Start a tmux session with the poker server configured for testing:

```bash
# Kill any existing tmux sessions
tmux kill-server 2>/dev/null || true

# Start server with infinite bankroll for long simulations
tmux new-session -d -s poker-server \
  'task server -- --infinite-bankroll --hands 50000 --timeout-ms 20 --npc-bots 3'

# Or with custom configuration
tmux new-session -d -s poker-server \
  'task server -- \
    --infinite-bankroll \
    --hands 100000 \
    --timeout-ms 50 \
    --small-blind 5 \
    --big-blind 10 \
    --start-chips 1000 \
    --npc-calling 1 \
    --npc-random 1 \
    --npc-aggro 1'

# With server-side statistics enabled (for bot development)
tmux new-session -d -s poker-server \
  'task server -- \
    --infinite-bankroll \
    --hands 10000 \
    --timeout-ms 20 \
    --npc-bots 3 \
    --collect-detailed-stats \
    --print-stats-on-exit'
```

### One-liner: Run server and complex bot together

This runs a 3k-hand session with 3 NPCs, spawns the complex bot once, prints stats on exit, and terminates automatically:

```bash
task server -- --hands 3000 --npc-bots 3 --collect-detailed-stats --print-stats-on-exit \
  --bot-cmd 'go run ./sdk/examples/complex'
```

Tip: add flags like `--timeout-ms 20`, `--collect-detailed-stats`, and `--print-stats-on-exit` as needed.

### 2. Monitor Server Logs

```bash
# View server logs in real-time
tmux attach -t poker-server

# Or capture recent logs
tmux capture-pane -t poker-server -p | tail -20

# Detach from tmux: Press Ctrl+B, then D
```

### 3. Run the Complex Bot

```bash
# Run the complex bot (it will auto-terminate when game completes)
go run ./sdk/examples/complex

# Or run in background
go run ./sdk/examples/complex &

# With debug logging
go run ./sdk/examples/complex --debug
```

### 4. Monitor Performance Metrics

#### Real-time Server Stats
```bash
# Check current game statistics (human-readable)
curl -s http://localhost:8080/stats

# Pretty-print the JSON snapshot for the default game
curl -s http://localhost:8080/admin/games/default/stats | jq .

# Watch stats update every 2 seconds
watch -n 2 'curl -s http://localhost:8080/admin/games/default/stats | jq .'
```

#### Game Configuration
```bash
# View active games
curl -s http://localhost:8080/games | jq .

# Check specific game stats (JSON/Markdown/Text)
curl -s http://localhost:8080/admin/games/{game}/stats | jq .
curl -s http://localhost:8080/admin/games/{game}/stats.md
curl -s http://localhost:8080/admin/games/{game}/stats.txt
```

#### Server-Side Performance Results

Results are generated by the server. Use the admin endpoints instead of bot-written files.

```bash
# Save JSON results for the default game
curl -s http://localhost:8080/admin/games/default/stats > results.json

# Extract the human player summary
cat results.json | jq '{
  hands_completed: .hands_completed,
  player: (.players[] | select(.role == "player") | {
    name: .display_name,
    net_chips: .net_chips,
    avg_per_hand: .avg_per_hand,
    bb_per_100: ((.avg_per_hand / (.big_blind // 10)) * 100? // adjust if needed)
  })
}'

# Compare all players (sorted by net chips)
cat results.json | jq '.players |
  sort_by(.net_chips) | reverse | .[] | {
    name: .display_name,
    role: .role,
    net_chips: .net_chips,
    bb_per_100: ((.avg_per_hand / 10) * 100),
    win_rate: ((.total_won / (.total_won + .total_lost)) * 100)
  }'

# Detailed statistics for the player (when server started with --collect-detailed-stats)
cat results.json | jq '.players[] | select(.role == "player") | .detailed_stats | {
  BB100: .bb_per_100,
  WinRate: .win_rate,
  ShowdownWinRate: .showdown_win_rate,
  Mean: .mean,
  StdDev: .std_dev
}'

# Position / Street / Hand category breakdown (detailed stats)
cat results.json | jq '.players[] | select(.role == "player") | .detailed_stats.position_stats'
cat results.json | jq '.players[] | select(.role == "player") | .detailed_stats.street_stats'
cat results.json | jq '.players[] | select(.role == "player") | .detailed_stats.hand_category_stats'

# Session timing
cat results.json | jq '{
  start: .start_time,
  end: .end_time,
  duration_seconds: .duration_seconds,
  hands_per_second: .hands_per_second
}'
```

## Advanced Configurations

### Infinite Bankroll Mode

Enable infinite bankroll to prevent bots from being eliminated:

```bash
# Bots never run out of chips
./server --infinite-bankroll --hands 100000
```

### Deterministic Testing

Use a fixed seed for reproducible games:

```bash
# Same shuffles and outcomes each run
./server --seed 12345 --hands 1000
```

### Custom NPC Mix

Control the types of NPC opponents:

```bash
# Specific NPC distribution
./server \
  --npc-calling 2 \  # 2 calling stations
  --npc-random 1 \   # 1 random bot
  --npc-aggro 3      # 3 aggressive bots

# Or auto-distribution
./server --npc-bots 6  # Automatically distributes strategies
```

### Performance Tuning

Adjust timeout for faster games:

```bash
# Ultra-fast games (20ms timeout)
./server --timeout-ms 20

# Standard games (100ms timeout)
./server --timeout-ms 100
```

### Server-Side Statistics

Enable detailed statistics collection for bot development:

```bash
# Detailed statistics (+ position and street analysis)
./server --collect-detailed-stats --print-stats-on-exit
```

**Note:** Statistics have minimal performance impact (~309ns per hand) but should be disabled in production for maximum throughput.

## Development Loop

### Typical Workflow

1. Run a session with detailed stats and print-on-exit (single command).
2. Read the Markdown summary printed on exit (or fetch /admin/games/default/stats.md).
3. Iterate: tweak bot or flags; rerun.

### Script Example

```bash
#!/bin/bash
# dev-loop.sh - Simplified automated testing loop (prints Markdown stats on exit)

# Configuration
HANDS=3000
TIMEOUT_MS=20
RUNS=5

mkdir -p results

for i in $(seq 1 $RUNS); do
  echo "Run $i of $RUNS"

  # Single command: server spawns the bot, prints stats on exit
  task server -- --hands $HANDS --timeout-ms $TIMEOUT_MS --npc-bots 3 \
    --collect-detailed-stats --print-stats-on-exit \
    --bot-cmd 'go run ./sdk/examples/complex' | tee results/run-$i.md

done

# Optional: extract player BB/100 from the printed Markdown
rg -n "^\| .*player.*\|" -r "$0" results/run-*.md
```

## Metrics to Track

### Key Performance Indicators

1. **BB/100** (Big Blinds per 100 hands)
   - Target: Positive value
   - Current: Complex bot loses ~2569 BB/100 (catastrophic!)

2. **Win Rate** (Percentage of hands won)
   - Target: 15-25% in 6-max
   - Current: Complex bot wins 11.4% (too low)
   - Track via: `.DetailedStats.WinRate`

3. **Showdown Win Rate**
   - Target: 50-60% when reaching showdown
   - Current: Complex bot at 56.4% (good!)
   - Track via: `.DetailedStats.ShowdownWinRate`

4. **Position Performance**
   - Button (BTN): Should be most profitable
   - Cutoff (CO): Second most profitable
   - Early positions: Tighter, less profitable
   - Track via: `.DetailedStats.PositionStats`

5. **Hand Category Analysis**
   - Premium hands: Should be highly profitable
   - Strong hands: Moderately profitable
   - Medium/Weak: Should mostly fold pre-flop
   - Track via: `.DetailedStats.HandCategoryStats`

6. **Street Analysis**
   - Where are hands ending?
   - Current: Complex bot plays 99.9% to showdown (major leak!)
   - Track via: `.DetailedStats.StreetStats`

### Debugging Tips

```bash
# Check if server is running
tmux list-sessions

# View all server logs
tmux capture-pane -t poker-server -p -S -

# Check for timeouts
curl -s http://localhost:8080/stats | grep -i timeout

# Monitor specific bot
tmux capture-pane -t poker-server -p | grep "complex-"
```

## Troubleshooting

### Common Issues

1. **Bot disconnects early**
   - Without infinite bankroll, bot runs out of chips
   - Solution: Use `--infinite-bankroll` flag

2. **Server not responding**
   - Check if tmux session exists: `tmux ls`
   - Check port: `lsof -i :8080`

3. **No results file generated**
   - Bot must receive `game_completed` message
   - Check `--hands` limit is set

4. **Slow performance**
   - Reduce timeout: `--timeout-ms 20`
   - Reduce debug logging
   - Close other applications

### Clean Up

```bash
# Kill all tmux sessions
tmux kill-server

# Remove result files
rm -f results/*.json

# Kill stuck processes
pkill -f "task server"
pkill -f "sdk/examples/complex"
```

## Example Analysis Session

```bash
# 1. Run a one-off session (prints Markdown stats on exit)
TASK_ARGS=(--hands 5000 --timeout-ms 20 --npc-bots 3 --collect-detailed-stats --print-stats-on-exit)
BOT_CMD="go run ./sdk/examples/complex"

task server -- "${TASK_ARGS[@]}" --bot-cmd "$BOT_CMD" | tee results/analysis.md

# 2. Optionally, fetch JSON stats
curl -s http://localhost:8080/admin/games/default/stats | jq . > results/analysis.json

# 3. Clean up
pkill -f "sdk/examples/complex" 2>/dev/null || true
```

## Next Steps

1. **Improve Bot Strategy**: Focus on beating aggressive NPCs
2. **Add Custom Metrics**: Track specific scenarios (bluffs, folds, etc.)
3. **Create Benchmarks**: Set performance targets
4. **Automate Testing**: Build scripts for repeated runs
5. **Visualize Results**: Create charts from JSON data
